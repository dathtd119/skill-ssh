# ============================================
# SSH Manager Skill - Server Configuration
# ============================================
# Copy this file to .env and fill with your actual server details
# NEVER commit the .env file to version control!

# --------------------------------------------
# SECURITY WARNINGS
# --------------------------------------------
# ⚠️  CRITICAL: Add .env to .gitignore immediately!
# ⚠️  Use chmod 600 .env to restrict file permissions
# ⚠️  Prefer SSH key authentication over passwords
# ⚠️  Rotate passwords regularly
# ⚠️  Never share .env files via chat, email, or public channels

# --------------------------------------------
# Example Server 1: Production (SSH Key Auth - RECOMMENDED)
# --------------------------------------------
SSH_SERVER_PRODUCTION_HOST=prod.example.com
SSH_SERVER_PRODUCTION_USER=admin
SSH_SERVER_PRODUCTION_KEYPATH=~/.ssh/id_rsa
SSH_SERVER_PRODUCTION_PORT=22
SSH_SERVER_PRODUCTION_DEFAULT_DIR=/var/www/html
SSH_SERVER_PRODUCTION_DESCRIPTION=Main production server

# Optional: Sudo password for automated deployments
# Leave empty if using NOPASSWD in sudoers (more secure)
# SSH_SERVER_PRODUCTION_SUDO_PASSWORD=sudo_password_here

# --------------------------------------------
# Example Server 2: Staging (Password Auth)
# --------------------------------------------
SSH_SERVER_STAGING_HOST=staging.example.com
SSH_SERVER_STAGING_USER=deploy
SSH_SERVER_STAGING_PASSWORD=your_secure_password_here
SSH_SERVER_STAGING_PORT=22
SSH_SERVER_STAGING_DEFAULT_DIR=/home/deploy/app
SSH_SERVER_STAGING_DESCRIPTION=Staging environment

# --------------------------------------------
# Example Server 3: Development (Custom Port)
# --------------------------------------------
SSH_SERVER_DEV_HOST=192.168.1.100
SSH_SERVER_DEV_USER=developer
SSH_SERVER_DEV_KEYPATH=~/.ssh/dev_key
SSH_SERVER_DEV_PORT=2222
SSH_SERVER_DEV_DEFAULT_DIR=/home/developer/projects
SSH_SERVER_DEV_DESCRIPTION=Local development server

# --------------------------------------------
# Server Naming Convention
# --------------------------------------------
# SSH_SERVER_[NAME]_[PROPERTY]
# 
# Where NAME is your server identifier (UPPERCASE)
# And PROPERTY can be:
#   HOST          - Server hostname or IP (required)
#   USER          - SSH username (required)
#   PASSWORD      - SSH password (for password auth)
#   KEYPATH       - Path to SSH private key (for key auth, recommended)
#   PORT          - SSH port (default: 22)
#   DEFAULT_DIR   - Default working directory (optional)
#   SUDO_PASSWORD - Sudo password for automated operations (optional)
#   DESCRIPTION   - Server description (optional)

# --------------------------------------------
# Authentication Methods (choose one per server)
# --------------------------------------------
# Method 1: SSH Key (RECOMMENDED - Most Secure)
#   SSH_SERVER_NAME_KEYPATH=~/.ssh/id_rsa
#   
# Method 2: Password (Less Secure)
#   SSH_SERVER_NAME_PASSWORD=your_password

# --------------------------------------------
# Sudo Configuration (3 options)
# --------------------------------------------
# Option 1: Pre-configured sudo password (for automation)
#   SSH_SERVER_NAME_SUDO_PASSWORD=sudo_pass
#   Use case: Automated deployments, scheduled tasks
#
# Option 2: No sudo password configured
#   Use case: Manual operations, will prompt at runtime
#
# Option 3: NOPASSWD in server's sudoers (RECOMMENDED for automation)
#   Configure on server: username ALL=(ALL) NOPASSWD: ALL
#   Use case: Most secure for automated scripts

# --------------------------------------------
# Quick Setup Guide
# --------------------------------------------
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your server details
# 3. Secure the file: chmod 600 .env
# 4. Add to .gitignore: echo ".env" >> .gitignore
# 5. Test connection: bun run scripts/core/list-servers.ts
